[package]
name = "rust_demo"
version = "0.2.0"
edition = "2021"
description = "A cross-platform command-line Hello World written in Rust"
license = "MIT"

[profile.release]
lto = true
codegen-units = 1
opt-level = "z"
strip = true